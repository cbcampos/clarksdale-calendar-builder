window.CalendarBuilderConfig = {
  // Add a browser-restricted Google Calendar API key here to enable live refresh
  // from GitHub Pages. Leave blank to use the bundled calendar-feed.ics snapshot.
  googleCalendarApiKey: "",
};
